#include <iostream>
#include <iostream>
#include <iomanip>
#include <cmath>

using long long ll;
using namespace std;

// (a+b) % c = ((a % c) + (b % c)) % c
// (a-b) % c = ((a % c) - (b % c)) % c
// (a*b) % c = ((a % c) * (b % c)) % c
// (a/b) % c = ((a % c) * (b^-1 % c)) % c

// tinh tong cac so tu 1 den n chia du cho 1000000007 (10^9)

int main()
{
    return true;
}